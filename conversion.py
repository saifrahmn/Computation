sig1,sig2 = input(print("Enter Σ \n")).split()
states=int(input(print("Enter number of states")))
q = []
for i in range(0,states):
    q[i]= input("Enter name of the states")
print(q)

for i in range(q):
    if q = something that never came before:
       # Make changes accordingly in nfa
    else:
       # check other states transition function
